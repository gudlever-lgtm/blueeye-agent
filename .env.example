# Convenience env file for running the agent locally or in Docker.
# In production these are usually set in /etc/blueeye/agent.conf instead.

BLUEEYE_SERVER_URL=http://localhost:8000
BLUEEYE_AGENT_TOKEN=paste-token-from-server-ui
BLUEEYE_POLL_INTERVAL=60
BLUEEYE_LOG_LEVEL=INFO
